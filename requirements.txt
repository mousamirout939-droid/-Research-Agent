# --- Core agent -------------------------------------------------------------
anthropic>=0.40.0,<1.0.0        # LLM provider (planner / writer / verifier)
pydantic>=2.6,<3.0              # typed data models (agent/models.py)
python-dotenv>=1.0.1,<2.0       # loads .env for local development

# --- Search & page reading ---------------------------------------------------
tavily-python>=0.5.0,<1.0.0     # primary search provider (agent-optimized)
ddgs>=9.0.0                     # key-free fallback search provider
httpx>=0.27,<1.0                # page fetching
trafilatura>=1.12,<2.0          # clean article-text extraction

# --- UI -----------------------------------------------------------------------
gradio>=6.0,<7.0                # demo UI (app.py)
markdown>=3.6,<4.0               # report markdown -> HTML rendering

# --- Eval & testing -------------------------------------------------------------
pytest>=8.0,<9.0
pytest-mock>=3.14,<4.0
tabulate>=0.9,<1.0               # pretty eval result tables
